<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.back.mapper.VideoMapper">

    <select id="queryVideoCover" resultType="com.back.entity.Video">
        select * from video where is_delete = 1 limit 5
    </select>

    <select id="querySearch" resultType="com.back.entity.Video">
        select * from video where is_delete = 1 and v_name like  "%"#{name}"%" order by times DESC
    </select>

    <update id="addTimes"  parameterType="java.lang.Integer" >
        update video set times = times + 1 where v_id =#{id}
    </update>
</mapper>
